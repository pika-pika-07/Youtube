import React from "react";
import Body from "./Body";
import Header from "./Header";

const AppContainer = () => {
  return (
    <div>
      <Header />
      <Body />
    </div>
  );
};

export default AppContainer;
